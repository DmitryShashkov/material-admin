export interface ArticleSettings {
    metaTitle: string;
    metaDescription: string;
    // tags: number[];
    // displayURL: string;
    // previewImage: File;
    // publishingDate: Date;
    // lastBreadcrumb: string;
}
